apiversion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region : us-east-1
  availabilityZones:
   - us-east-1a
   - us-east-1b

managedNodeGroups:
  - name: expense
    instanceTypes: ["m5.large","c3.large","c4.large",c5.large"]
    desiredCapacity: 2
    spot: true

